#include<stdio.h>
#include<stdlib.h>
void main(){
    int a[10],key,i,n;
    printf("enter n");
    scanf("%d",&n);
    printf("enter key");
    scanf("%d",&key);
    for(int i=0;i<n;i++)
    {
      printf("enter  %d:",i+1);
      scanf("%d",&a[i]);
    }
   
    for(int i=0;i<n;i++)
    {
      if(a[i]==key)
      {
        printf("%d key found",i);
        exit(0);
      }
      }
    printf("key not found");
    
 }
