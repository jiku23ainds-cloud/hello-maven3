#include<stdio.h>
int main()
{
int key,n=5;
int a[5]={10,20,30,40,50};
int flag=1;

printf("Enter the element you wants to search:");
scanf("%d",&key);
for(int i=0;i<=n;i++){
 if(key==a[i]){
 printf("key found");
 flag=0;
 }}

if(flag==1){
 printf("key is not found");
 }}
