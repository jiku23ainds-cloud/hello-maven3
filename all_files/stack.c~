// stack operation
#include<stdio.h>
#include<stdlib.h>
#define MAX 5
int stack[MAX];
int item;
int i=-1;
int j,f=0;

void push()
{
	if(top==MAX-1)
	printf("STACK OVERFLOW\n");
	else
	{
		top++;
		printf("enter values :\n");
		scanf("%d",&item);
		stack[top]=item;
	}
}
void pop()
{
	if(top==-1)
	printf("STACK UNDERFLOW\n");
	else
	{
		printf("Element poped is %d\n",stack[top]);
		top--;
	}
}
void ispalindrome()
{
	int top=i;
	for(int y=0;y<=top&&top>=0;y++,top--)
	{
		if(stack[y]!=stack[top])
		{
			f=0;break;}
			else{
			f=1;
			}
		if(f==1)
		{
			printf("it is a palindrome");}
		else{
		printf("It is not a palidrome");
		}
				
void display()
{
	for(int i=0;i<=top;i++)
	printf("%d\n",stack[i]);
}
void main()
{
 int a;
	while(1)
	{
		printf("---------MENU---------\n");
		printf("To Push an Element to stack press 1\n");
		printf("To Pop an Element from stack press 2\n");
		printf("To display press 3\n");
		printf("To exit press 4 \n");
		printf("Enter the choice : ");
		scanf("%d",&a);
		switch (a)
		{
			case 1:push();
			break;
			case 2:pop();
			break;
			case 3:display();
			break;
			case 4:
			break;
		}
	}
}
