#include<stdio.h>
#include<ctype.h>
int top=1;
char stack[100];
int j,f=0;
void push(char j){
	if(top==(MAX-1)){
	printf("stack overflow");
	}
	else{
	top++;
	stack[top]=j:
	}
  }
char pop(){
  if(top==-1){
   return -1;
  else{
   return stack[top--];
   }
  }
int prec(char x){
 if(x=='('){
  return 0;
  }
 else if(x=='+'|x=='-'){
  return 1;
  }
 else if(x=='*'|x=='/'){
  return 2;
  }
 else if(x=='^'){
  return 3;
  }
 }
void main(){
  char str;
  int i=0;
  printf("Enter the expression : ");
  scanf("%s",str);
  while(str[i]!='\0'){
   if  
   
